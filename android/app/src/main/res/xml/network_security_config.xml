<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 🛡️ PRODUCTION HARDENING: Disable cleartext traffic (HTTP) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Explicitly trust the API domain -->
    <domain-config>
        <domain includeSubdomains="true">tipsytheoryy.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
